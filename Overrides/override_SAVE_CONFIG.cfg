[gcode_macro SAVE_CONFIG]
rename_existing: SAVE_CONFIG_BASE
gcode:
  {% if params.RESTART is not defined or params.RESTART == 1 %}
    M84
  {% endif %}
  SAVE_CONFIG_BASE {% for p in params
                %}{'%s=%s ' % (p, params[p])}{%
               endfor %}

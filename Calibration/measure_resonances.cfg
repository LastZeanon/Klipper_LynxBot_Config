[gcode_macro QUERY_ACCELEROMETER]
gcode:
  ACCELEROMETER_QUERY

[gcode_macro MEASURE_RESONANCES]
gcode:
  BACKUP

  G28 X0 Y0 Z0 P0

  SHAPER_CALIBRATE {% for p in params
                %}{'%s=%s ' % (p, params[p])}{%
               endfor %}

  M300 A3  
